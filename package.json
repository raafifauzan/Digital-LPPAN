{
  "name": "monitoring-digitalisasi",
  "version": "1.0.0",
  "private": true,
  "description": "Digitalization Monitoring Dashboard with Ask AI endpoint",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check public/app.js && node --check server.js && node --check api/ask.js && node --check api/data.js"
  },
  "dependencies": {
    "@google/genai": "^1.40.0",
    "dotenv": "^17.2.4",
    "express": "^5.2.1"
  }
}
